# SPDX-License-Identifier: MIT
# For license details, see the repository root LICENSE file.
---
name: Security - Dependency Review

'on':
  pull_request:
    branches: [main]
  merge_group:

permissions: {}

concurrency:
  group: dependency-review-${{ github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-dependency-review.yml@43325346d688be984d6bde155603d18fbdd89278 # v0.33.0
    with:
      fail-on-severity: high
      tooling-ref: '43325346d688be984d6bde155603d18fbdd89278' # v0.33.0
      egress-policy: block
      egress-preset: scorecard
    permissions:
      # read: fetch manifest lockfiles for dependency comparison
      contents: read
      # read: read PR metadata for dependency-review-action
      pull-requests: read
