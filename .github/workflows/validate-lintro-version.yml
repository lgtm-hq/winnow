# SPDX-License-Identifier: MIT
---
name: Quality Check - Lintro Version Sync

'on':
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'pyproject.toml'
      - '.github/workflows/quality-ci.yml'
      - '.github/workflows/validate-lintro-version.yml'
  merge_group:
  push:
    branches: [main]
    paths:
      - 'pyproject.toml'
      - '.github/workflows/quality-ci.yml'
      - '.github/workflows/validate-lintro-version.yml'
  workflow_dispatch:

permissions: {}

concurrency:
  group: validate-lintro-version-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    # yamllint disable-line rule:line-length
    uses: lgtm-hq/lgtm-ci/.github/workflows/reusable-validate-lintro-version.yml@43325346d688be984d6bde155603d18fbdd89278 # v0.33.0
    with:
      tooling-ref: '43325346d688be984d6bde155603d18fbdd89278' # v0.33.0
      egress-policy: block
      egress-preset: github-tooling
    permissions:
      # read: read pyproject.toml and workflow files for version sync checks
      contents: read
      # read: resolve lintro package metadata from GHCR
      packages: read
